services:
  web:
    build:
      context: ../../
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT}:8080"
    environment:
      - REDIS_URL=${REDIS_URL}
  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
volumes:
  redis-data:
